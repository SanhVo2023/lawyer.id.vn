/* This file is regenerated locally by `payload` dev mode but committed
 * for production builds (Netlify) where the Payload CLI cannot run.
 * No custom admin components are registered, so the map is empty.
 */
export const importMap = {}
